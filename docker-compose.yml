version: '3'

services:
  db:
    image: postgres:14.5
    ports:
      - 5432:5432
    volumes:
      - db_volume:/var/lib/postgresql/data
      - ./databases/:/databases/
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust

volumes:
  db_volume:
    external: true